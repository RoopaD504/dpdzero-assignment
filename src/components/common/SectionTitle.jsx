import React from "react";

const SectionTitle = ({ title }) => {
  return (
    <h2 className="text-4xl pb-2 font-bold m-4 text-[#027764] text-start">
      {title}
    </h2>
  );
};

export default SectionTitle;
